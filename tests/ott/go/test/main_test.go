package test

/*
import (
	"os"
	"testing"
)

var (
	phoenixServerMock *MockHttpServer
	globalPhoenixUrl string
)

// global setup&teardown for the package
func TestMain(m *testing.M) {
	SetLogLevel("info")
	phoenixServerMock = StartMockHttpServer()
	r := startRest()
	globalPhoenixUrl = localHttpUrl(r)
	c := m.Run()
	phoenixServerMock.Close()
	os.Exit(c)
}

 */